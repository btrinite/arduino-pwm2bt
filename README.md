# arduino-pwm2bt
Arduino to act as a gateway between bluetooth and PWM to control RC Car (get PWM order from Rx controller, sent it to BT, then get PWM order from BT and drive PWM output to pilot throttle/steering)
